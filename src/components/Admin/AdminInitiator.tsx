"use client";

interface AdminInitiatorProps {
  children: React.ReactNode;
}

const AdminInitiator = ({ children }: AdminInitiatorProps) => {
  return <>{children}</>;
};

export default AdminInitiator;
